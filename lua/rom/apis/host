--something short to keep me from writing this all the time. might expand in the future

function getID()
  local r = os.getComputerLabel()
  if r == nil then
    if turtle then
      r = "turtle"..os.getComputerID()
    else
      r = "computer"..os.getComputerID()
    end
  end
  return r
end

